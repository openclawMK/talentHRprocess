Drop your sample CVs here (PDF or DOCX) to test the parsing engine.

Then tell Claude "test the samples" and they'll be run through
POST /api/upload-cv and the parsed results shown back to you.

Supported: .pdf, .docx   (images are rejected by design)
Max size: 5MB per file
